import 'package:flutter/material.dart';

class AppColors {
  // Ana renkler - daha canlı tonlar
  static const primaryGreen = Color(0xFF4CD964); // Neon yeşil tonu
  static const primaryOrange = Color(0xFFFF7F50); // Canlı turuncu

  // Arka plan renkleri - hafif koyulaştırılmış
  static const backgroundGreen = Color(0xFFDCE8DD); // Daha koyu yeşilimsi gri
  static const secondaryBackground = Color(0xFFF0F2F0); // Hafif gri tonlu beyaz
  static const surfaceColor = Color(0xFFF5F7F5); // Kartlar için yüzey rengi

  // Gölge renkleri - daha belirgin
  static const neonGreenShadow = Color(0xFF45FF57); // Parlak neon yeşil gölge
  static const neonOrangeShadow =
      Color(0xFFFFAB91); // Parlak neon turuncu gölge

  // Metin renkleri - kontrast artırıldı
  static const textDark = Color(0xFF0D3F15); // Daha koyu yeşil
  static const textLight = Color(0xFFF8F9F8); // Çok açık gri-beyaz
  static const textMuted = Color(0xFF6B756D); // Orta ton gri

  // Vurgu renkleri - daha canlı
  static const accentGreen = Color(0xFF00E676); // Parlak vurgu yeşili
  static const accentOrange = Color(0xFFFFAB40); // Parlak vurgu turuncusu

  // Durum renkleri
  static const success = Color(0xFF00C853);
  static const error = Color(0xFFFF5252);
  static const warning = Color(0xFFFFD740);
  static const info = Color(0xFF40C4FF);

  // Gölge opaklıkları
  static const cardShadowOpacity = 0.15;
  static const buttonShadowOpacity = 0.25;
  static const neonShadowOpacity = 0.35;
}
